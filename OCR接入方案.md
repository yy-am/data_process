# OCR 接入方案

## 1. 文档目的

本文档定义图片 OCR 在当前 Python PoC 中的接入边界和实施路径。

## 2. OCR 的职责

- 从表格类图片中抽取文本
- 恢复足够可用的行列结构
- 保留置信度信息
- 将识别结果转换成统一 `InputSnapshot`

OCR 不负责模板选择、规则检索和规则执行。

## 3. 当前阶段边界

第一阶段范围：

- 一张图片内有一个主表格
- 图片清晰度可接受
- 行列结构可恢复

第一阶段不包含：

- 一张图多个无关表格
- 复杂混排版面理解
- 手写表单

## 4. 推荐接入流程

```text
图片文件
  -> OCR 提供方适配器
  -> 规范化 OCR 文本块
  -> 表格重建器
  -> 图片快照构建器
  -> InputSnapshot
```

## 5. 推荐内部组件

### `provider_adapter.py`

- 调用选定 OCR 方案
- 返回统一后的文本块结果

### `table_rebuilder.py`

- 按 Y 轴聚合成行
- 按 X 轴估算列
- 识别候选表头行
- 将单元格映射到重建后的列带

### `ocr_snapshot_builder.py`

- 规范化表头
- 附加置信度
- 构建统一输入快照

## 6. OCR 选型标准

- 表格文本识别质量
- 是否提供边界框
- 是否提供置信度
- Python 集成成本
- 本地或服务化部署可行性
- 对打印类多语言表头的支持能力

## 7. 当前建议方向

优先建议先尝试 `PaddleOCR`，原因是：

- Python 生态适配较好
- 开源
- 支持边界框和置信度
- 适合本地快速验证

但最终方案仍需在正式开发前确认。

## 7.1 当前 PoC 的图片特例

针对“税局网站截图”这个当前唯一图片场景，PoC 已允许走一条更直接的链路：

```text
税局网站截图
  -> qwen-vl 模型识别
  -> 提取可见字段和结构化键值
  -> 生成统一 InputSnapshot
  -> 走单一固定映射逻辑
  -> 直接产出预览结果
```

这个特例的前提是：

- 当前图片场景只有一种映射逻辑
- 图片来源固定为税局网站截图
- 当前目标是先验证“识别 + 映射”能否跑通

因此当前 PoC 对税局截图不强制先做传统 OCR provider + table rebuilder，再做复杂规则检索。

当前前端控制台也已经补齐了这条图片链的最小入口：

- 图片 OCR 模型配置
- 图片上传创建任务
- 运行固定映射加工
- 预览摘要与预览分页查看

## 8. 置信度处理要求

若关键表头置信度过低：

- 不能默默信任
- 必须进入单轮确认包
- 若低到无法安全推进，则进入 `NEED_SOURCE_FIX`

## 9. 明确禁止的 OCR 捷径

- 针对某张图片硬编码预期表头
- 跳过置信度处理
- 未经检索直接把 OCR 文本映射成模板字段
- 用图片专属分支伪造结构恢复
- 默默用常量补齐 OCR 缺失表头
