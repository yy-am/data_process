# 接口设计

## 1. 文档目的

本文档定义当前 PoC 的 HTTP 接口契约。

## 2. 基础约定

- 基础路径：`/api/v1`
- 普通请求：`application/json`
- 上传请求：`multipart/form-data`
- 响应：`application/json`

通用响应结构：

```json
{
  "code": "OK",
  "message": "成功",
  "data": {}
}
```

## 3. 核心接口

### 3.1 上传并创建任务

`POST /api/v1/tasks/upload`

表单字段：

- `file`
- `inputType`

### 3.2 查询任务摘要

`GET /api/v1/tasks/{taskId}`

### 3.3 查询输入快照

`GET /api/v1/tasks/{taskId}/input-snapshot`

### 3.4 查询模板候选

`GET /api/v1/tasks/{taskId}/template-candidates`

说明：

- 当前返回的是 `template_catalog.md` 中的模板条目摘要
- 不代表本地打分检索结果
- 主要作用是给前端查看当前 catalog 内容，以及给模板识别 Agent 提供辅助摘要

### 3.5 查询规则候选

`GET /api/v1/tasks/{taskId}/rule-candidates`

### 3.6 查询确认包

`GET /api/v1/tasks/{taskId}/confirmation-package`

### 3.7 提交确认结果

`POST /api/v1/tasks/{taskId}/confirmation`

### 3.8 触发转换

`POST /api/v1/tasks/{taskId}/run`

### 3.9 查询预览汇总

`GET /api/v1/tasks/{taskId}/preview-summary`

### 3.10 分页查询预览行

`GET /api/v1/tasks/{taskId}/preview-rows`

### 3.11 查询单行详情

`GET /api/v1/tasks/{taskId}/preview-rows/{rowNo}`

### 3.12 最终确认预览结果

`POST /api/v1/tasks/{taskId}/final-confirm`

### 3.13 导出结果

`POST /api/v1/tasks/{taskId}/export`

### 3.14 下载导出文件

`GET /api/v1/tasks/{taskId}/export-file`

### 3.15 上传知识包

`POST /api/v1/kb/templates/bundle`

表单字段：

- `scene`
- `country`
- `specFile`
- `instructionText` 可选

当前 `specFile` 约定：

- 第 1 行：目标字段列
- 第 2 行：模板列

接口行为：

1. 解析第 2 行模板列
2. 更新 `knowledge_base/template_catalog.md`
3. 基于第 1 行和第 2 行生成基础映射规则
4. 将规则写入 `knowledge_base/<scene>/<country>/rule.json`

## 4. 接口护栏

- 任务被阻断时，禁止伪装成成功
- 未解决规则草稿前，禁止自动执行转换
- 导出必须基于确认后的 staging 版本
- 禁止从原始输入或临时内存结果直接导出
- 模板识别只能从 `template_catalog.md` 中选择模板、场景和国家

## 5. 关键错误码

- `UNSUPPORTED_FILE_TYPE`
- `HEADER_NOT_DETECTED`
- `TEMPLATE_NOT_RESOLVED`
- `RULE_NOT_RESOLVED`
- `BLOCKING_ISSUES_EXIST`
- `TASK_STATUS_INVALID`
- `PREVIEW_NOT_READY`
- `EXPORT_NOT_ALLOWED`
