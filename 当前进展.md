# 当前进展

## 日期

2026-05-06

## 当前状态

已完成当前 Python PoC 的核心演示闭环：

- Excel 输入链路可真实完成上传、输入快照、模板目录识别、规则候选
- 税局网站截图图片链路可真实完成 qwen-vl 识别、固定映射和预览结果
- 前端控制台已可直接配置模型、上传文件并触发主要流程
- 模型选型脚本与 Excel 报告产物已具备，可用于快速生成 PoC 测试报告

## 中文正式文档入口

1. [方案设计.md](/D:/lsy_projects/data_process/方案设计.md)
2. [开发规范.md](/D:/lsy_projects/data_process/开发规范.md)
3. [当前进展.md](/D:/lsy_projects/data_process/当前进展.md)
4. [数据库设计.md](/D:/lsy_projects/data_process/数据库设计.md)
5. [接口设计.md](/D:/lsy_projects/data_process/接口设计.md)
6. [模块结构设计.md](/D:/lsy_projects/data_process/模块结构设计.md)
7. [OCR接入方案.md](/D:/lsy_projects/data_process/OCR接入方案.md)

## 已确认范围

- 先做 Python PoC
- 后续正式上线迁移到 Java
- Excel 和图片 OCR 输入都必须支持
- Excel 模板检索必须以 sheet 列名为核心
- 文件名和 sheet 名不是模板识别主信号
- 标准模板和场景/国家映射规则都来自知识库
- 当前只做单轮确认
- 当前阶段不做历史确认记忆复用
- 当前阶段不做复杂表达式体系
- 当前阶段不做完整规则管理后台

## 已确认护栏

- 每个新会话都必须先读 `方案设计.md`、`开发规范.md`、`当前进展.md`
- 禁止隐藏兜底逻辑
- 禁止用硬编码分支冒充 AI 智能
- 禁止绕过已批准分层
- 禁止静默修改契约、模型和流程
- 当规则模型无法支持某个场景时，禁止偷偷补“尽量跑通”的分支

## 已完成内容

- 已形成中文化的方案、规范、进展、数据库、接口、模块结构和 OCR 文档
- 英文文件名文档保留为兼容副本，后续不再作为正式入口使用
- 已补充规范：默认不再采用“保留旧副本 + 另建新正式入口”的方式处理文档迁移，后续应优先原位更新并减少冗余文件
- 已创建 Python 项目基础结构：`app/`、`tests/` 及分层子目录
- 已创建 FastAPI 入口 `app/main.py`，接入统一异常返回
- 已落地任务相关核心枚举与 schema：`InputType`、`TaskStatus`、`InputSnapshot`、确认包、预览结构、通用响应结构
- 已落地 `/api/v1/tasks` 路由骨架，覆盖接口设计文档中的全部核心接口路径
- 已实现最小任务服务骨架：当前只支持“上传创建任务”和“查询任务摘要”
- 上传接口当前只负责接收并落盘原始文件到 `storage/uploads/`，不在上传阶段提前按后缀拦截
- Excel 输入已接通首条真实解析链路：`upload -> .xlsx 内容解析 -> InputSnapshot 入库（内存版） -> 任务状态推进到 INPUT_PARSED`
- 已接通首条模板检索链路：`input snapshot -> 表头归一化匹配 -> 模板候选打分 -> 查询结果缓存（内存版）`
- 模板知识事实源已切到 `knowledge_base/template_catalog.md`：当前模板识别以 catalog 中的模板列名目录为准，不再依赖扫描所有模板 Excel 文件
- 已接通首条规则检索链路：`template_code + scene_code + country_code + source_type + status -> 规则候选过滤与排序 -> 查询结果缓存（内存版）`
- 规则检索已支持从命中的模板直接解析 `scene/country/rule`，规则真值来源改为知识库目录下同级 `rule.json`
- 已补齐模板识别 Agent 的正式结果结构、结果仓库与服务挂点；当前未接入真实 AI，因此系统不会从模板候选排序中偷偷推断 `template_code`、`scene_code`、`country_code`
- 对未实现能力采用显式拒绝策略：解析、检索、确认、执行、staging、导出均返回明确未实现或状态不合法，不提供伪结果
- 已新增首版 PostgreSQL 建表脚本 `sql/001_init.sql`，覆盖任务域、知识库、staging 与导出核心表
- 已补充解析层、OCR 层、检索层、Agent 层、规则引擎层的正式 Python 接口骨架，后续实现可直接挂接
- 已补充 `app/models/` 下的核心持久化记录模型，字段语义与数据库设计对齐
- 已新增内存版 `SnapshotRepository`，`GET /api/v1/tasks/{taskId}/input-snapshot` 现在可返回已解析的 Excel 快照
- 已新增内存版知识库与模板检索结果仓库；当前若知识库为空，模板候选会明确返回空列表，不会伪造匹配结果
- 已新增内存版规则知识库与规则候选结果仓库；规则检索不做任何场景/国家硬编码，只依赖知识库精确过滤
- 已新增模板识别结果 schema 与 repository，为后续真实 AI 接入保留稳定边界
- 已接入首批内存版知识库 seed 数据，当前包含 2 个模板和 2 条规则，可用于验证模板检索与规则检索链路
- 已补充模板识别 LLM client 协议与运行时配置边界；默认未配置时返回 `LLM_CLIENT_NOT_CONFIGURED`，不会伪造识别结果
- 已新增最小模板知识导入入口：可上传两行知识包 Excel，并即时更新 `template_catalog.md` 与对应 `rule.json`
- 已新增模板识别 Agent 运行时配置入口，可在 PoC 里快速切换 `provider/model/endpoint/apiKeyEnvVar`
- 模板识别 Agent 运行时配置现支持直接传 `apiKey` 或通过 `apiKeyEnvVar` 引用环境变量，便于 PoC 快速切换模型
- 已新增模板识别触发入口，便于围绕同一输入任务多次切换模型并观察识别结果
- 已新增模板识别提示协议与通用 HTTP 模型客户端，当前支持以 `openai_compatible_chat` 方式调用外部模型接口，并将返回结果严格校验为固定 JSON 结构
- 已新增规则草稿 Agent：支持独立模型配置、固定提示协议、真实模型调用入口和结构化 JSON 输出校验
- 任务流已开始向“KB 模板列匹配优先”收敛：唯一最高分模板会直接带出 `templateCode/sceneCode/countryCode`，只有无法唯一命中时才保留 Agent 介入空间
- 已新增统一知识库目录 `knowledge_base/<scene>/<country>/`，规则仍按目录维护；模板知识则统一收敛到 `knowledge_base/template_catalog.md`
- 已新增“知识包上传”最小链路：上传一个 Excel，按第 1 行目标列、第 2 行模板列自动更新 `template_catalog.md` 并生成基础映射规则写入知识库
- 知识包上传当前支持附带自然语言 `instructionText`，用于补充简单转换、计算和清洗规则说明，并一并写入 `rule.json`
- 已修正常见一致性问题：知识包上传后不再生成模板 Excel；模板与规则的运行时对齐改由 `templateCode + template_catalog.md + rule.json` 保证
- 已完成本地接口验证：知识包上传可成功生成模板与规则；模板列表不会混入 `mapping_spec.xlsx`
- 已恢复当前项目可用 Python 入口：可通过 `python.cmd` / `start-server.cmd` / `start-server.ps1` 启动和调试当前 FastAPI 项目
- 已打通真实模板识别模型调用：`template_catalog.md + deepseek-v3.1` 可对全部现有 Excel 测试样例进行真实识别，不再依赖任何本地猜测
- 已修复真实模型调用中的代理问题：LLM HTTP 客户端现在默认禁用错误系统代理，避免请求被误转发到 `127.0.0.1:9`
- 已新增税局网站截图图片链路：支持配置 qwen-vl 图片 OCR 模型、上传图片生成 `InputSnapshot`、返回固定图片模板候选与固定规则候选
- 已新增税局截图单一映射逻辑：图片任务可直接执行 `run -> preview-summary -> preview-rows`，产出固定目标 schema 的预览结果
- 已用真实 qwen-vl 模型验证通过一张模拟税局截图：图片字段识别、结构提取、固定映射、预览结果全部打通
- 已重做前端控制台首页：统一白色与浅蓝色视觉，修复原页面乱码，并补齐图片 OCR 配置、图片任务运行和预览结果入口
- 已生成 Excel PoC 测试结果工作簿：覆盖 11 份 Excel 输入样例，登记输入快照、真实模板识别结果与规则候选结果，产物在 `reports/excel_poc_test_report_20260430.xlsx`
- 已新增大模型对比测试脚本：支持切换 `endpointUrl / apiKey / modelName`，自动跑完整 Excel 样例集并输出对比型 Excel 报告，脚本为 `scripts/generate_model_comparison_report.py`
- 已补齐模型选型运行入口：新增正式配置文件 `scripts/model_eval_config.json`，以及一键运行脚本 `scripts/run_model_comparison.ps1` / `scripts/run_model_comparison.cmd`
- 已同步补充模型选型相关文档约束：方案设计中明确允许通过正式脚本批量跑样例并生成多模型对比报告

## 尚未开始或尚未完成

- 数据库建表实现
- 持久化 repository 实现
- OCR 适配器实现
- `.xls` 老格式支持
- 图片 OCR 解析实现
- 模板/规则知识检索进一步增强
- `template_code`、`scene_code`、`country_code` 的真实解析/确认链路
- 单轮确认包生成与确认结果落库
- Excel 规则引擎实现
- staging 预览实现
- 最终确认与导出实现
- 知识包上传后的自然语言规则补充尚未结构化落成 DSL，只是先保存在知识库规则文件中
- 图片真实截图样本与更多字段变体尚未补齐

## 本轮验证情况

- 已完成静态结构检查和人工核对
- 已完成本地 `TestClient` 级别接口验证：
  - 知识包上传接口成功
  - `template_catalog.md` 自动迁移生成成功
  - 上传后 catalog 自动追加成功
  - 自动生成 `rule.json` 成功
  - 模板列表读取成功
- 已确认一个真实边界：当两个模板列集合完全一致时，系统不会偷偷选一个作为最终模板，而是保持未解析状态
- 已完成真实模型级联调：
  - 全部现有 Excel 输入样例已跑真实模板识别
  - 税局网站截图模拟图已跑真实 qwen-vl 识别和固定映射预览

## 建议下一步

1. 继续补充更多 Excel 测试样例和更多候选模型，直接用现有对比脚本生成多轮选型报告
2. 继续把知识包上传后的自然语言补充规则结构化，形成可执行 DSL 草稿
3. 为税局网站真实截图补更多样本，校验 qwen-vl 在不同截图风格和分辨率下的稳定性
4. 视 PoC 进展决定是否把当前内存态结果迁移到数据库持久化
5. 确定并接入首版通用 OCR 方案，优先验证 `PaddleOCR`
6. 明确 `.xls` 是否纳入第一阶段，如纳入则补老格式解析方案
