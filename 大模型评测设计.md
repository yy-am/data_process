# 大模型评测设计

## 1. 目标

本文件补充两类新增 AI 需求的大模型效果评测设计：

1. 根据已有对账场景自动创建对账作业
2. 根据系统计算出的对账结果和预置 SOP 规则分析差异原因

本轮目标不是先把完整产品流程做完，而是先建立一套可快速复用的模型评测闭环：

- 有固定测试集
- 有统一输出 schema
- 有可重复运行的脚本
- 有可横向对比多个模型的评分规则

## 2. 为什么建议做最小 Demo

建议做一个最小独立评测 demo，而且复杂度不高，原因如下：

- 这两类需求都主要考验大模型的理解、结构化提取、归因和风险提示能力
- 它们暂时不依赖现有 Excel 上传和规则执行主链路，因此没必要先绑进完整后端
- 用独立脚本评测更快，便于频繁更换模型、提示词和参数
- 如果后续需要迁移到 Java 或 Spring AI Alibaba，测试集和评分规则仍可复用

当前仓库已经有模型评测脚本思路，因此新增这部分的最小成本是：

- 新增结构化测试集
- 新增针对两类任务的 prompt 与评分逻辑
- 新增评测报告输出

不建议直接编造“模型优劣结论”作为正式测评结果。没有真实 API 跑出来的数据，就只能写“预期表现”和“待实测假设”，不能冒充实测。

## 3. 本轮评测范围

### 3.1 任务 A：根据已有对账场景自动创建对账作业

评测模型是否能从已有对账场景需求中稳定抽取出结构化作业定义，包括但不限于：

- 对账对象
- 源系统与目标系统
- 关键匹配键
- 对账指标
- 过滤条件
- 调度频率
- 容差规则
- 是否缺关键信息，需要追问

### 3.2 任务 B：根据系统结果和预置 SOP 分析差异原因

评测模型是否能基于系统计算出的对账结果摘要、异常分布和预置 SOP，输出：

- 命中的分析 SOP
- 命中的规则检查项
- 主要差异原因标签
- 证据说明
- 是否需要人工介入
- 建议动作

## 4. 统一评测方法

### 4.1 输出约束

所有模型都必须输出 JSON，禁止自由散文式回答作为最终答案。

这样做的目的：

- 方便自动打分
- 降低不同模型话术差异对评测的干扰
- 便于后续直接接入服务端

### 4.2 评分原则

#### A 类任务：建对账作业

总分 100，建议按以下维度：

- JSON 可解析性：20
- 意图识别正确：10
- 是否正确判断“需要补充信息”：20
- 系统识别覆盖度：20
- 匹配键识别覆盖度：15
- 对账指标识别覆盖度：10
- 调度、容差、过滤条件识别：5

#### B 类任务：差异原因分析

总分 100，建议按以下维度：

- JSON 可解析性：20
- SOP 识别命中度：10
- SOP 检查项命中度：10
- 主原因标签命中度：30
- 证据关键词覆盖度：20
- 是否正确判断需要人工复核：10
- 建议动作覆盖度：10

## 5. 评测数据设计

本轮已补一套结构化测试集：

- `scripts/reconciliation_eval_cases.json`

覆盖：

- 根据已有对账场景建作业：18 个用例
- 根据系统结果 + SOP 分析差异原因：18 个用例

合计 36 个用例，属于可快速首轮选型的最小“几十个”规模。

其中差异原因分析用例已开始引入 SOP 化设计，允许每个场景预置：

- `sopCode`
- 场景说明
- 规则检查项
- 必须命中的检查项

这样评测的不只是“大模型会不会解释”，还包括：

- 能否识别当前差异最匹配哪条分析路径
- 能否从输入证据中触发正确检查项
- 能否基于 SOP 给出更稳定的下一步动作

## 6. 建议比较的模型

建议按同一温度、同一输出 schema、同一测试集横向比较以下模型：

- deepseek
- glm5
- qwen3-32b
- qwen-v3.5-35b

注意：

- 真实可用的 `endpointUrl`、`modelName` 以你实际拿到的供应商配置为准
- 不同平台对同名模型的 system prompt 支持、`response_format` 支持、超时行为可能不同
- 因此测试脚本优先做“兼容 openai chat completion”调用，不做平台强绑定

## 7. 结果解读建议

不要只看平均分，至少同时看以下维度：

- 总分均值
- 两类任务分开均值
- JSON 失败率
- 明确场景命中率
- 歧义场景“主动要求补充信息”的准确率
- 差异分析里的主因 top1 命中率
- 差异分析里的误报率

## 8. 建议的实施顺序

1. 先用本轮补的 36 个样例做首轮离线评测
2. 从四个模型里选出 1 到 2 个候选
3. 再补 10 到 20 个贴近真实业务语料的内部样例做第二轮复测
4. 再决定是否把获胜模型正式接入服务端

## 9. 当前产物

本轮已补：

- 评测设计文档：`大模型评测设计.md`
- 结构化测试集：`scripts/reconciliation_eval_cases.json`
- 最小评测脚本：`scripts/run_reconciliation_eval.py`
- 配置样例：`scripts/reconciliation_eval_config.example.json`
- deepseek 单模型配置：`scripts/reconciliation_eval_config.deepseek.json`

进一步统一后的入口：

- 统一用例目录：`scripts/eval_cases/`
- 三类用例清单：
  - `data_processing_template_identification.json`
  - `reconciliation_job_creation_from_scene.json`
  - `reconciliation_diff_analysis_from_result_and_sop.json`
- 统一 suite 清单：`scripts/eval_cases/manifest.json`
- 统一模型配置：`scripts/unified_model_eval_config.json`
- 统一配置样例：`scripts/unified_model_eval_config.example.json`
- 统一执行脚本：`scripts/run_unified_model_eval.py`

统一执行脚本会在一轮运行中完成：

1. 数据加工模板识别评测
2. 根据已有对账场景建作业评测
3. 根据系统结果 + SOP 分析差异原因评测

并输出一份统一报告，报告中会区分：

- 不同大模型
- 三种用例类型
- 每条用例在不同模型下的表现

## 10. 关于“是否可以编造测评结果”

可以写“预期表现判断”，但不能冒充“真实测评结果”。

允许的写法：

- “基于经验预期，模型 A 在结构化抽取上可能更稳，模型 B 在归因解释上可能更自然，待真实 API 跑数验证”

不允许的写法：

- “deepseek 92 分、glm5 88 分、qwen3-32b 85 分、qwen-v3.5-35b 90 分”
  前提却没有任何真实调用记录和评测报告产物

如果你后面给我 API 和 key，我建议直接跑真实报告；这一步从工程复杂度上看是可快速完成的，不值得伪造。
