# 方案设计

## 1. 文档目的

本文档是当前 Python PoC 的唯一设计事实来源。

后续所有实现、讨论、修改，都必须以本文件为准。

## 2. 已确认范围

- 先做 Python PoC
- 后续正式上线迁移到 Java
- Excel 和图片 OCR 输入都必须支持
- Excel 模板检索必须以 sheet 列名为核心
- 文件名和 sheet 名不是模板识别主信号
- 标准模板和场景/国家映射规则都来自知识库
- 当前只做单轮确认
- 当前阶段不做历史确认记忆复用
- 当前阶段不做复杂表达式体系
- 当前阶段不做完整规则管理后台

## 3. 核心原则

1. AI 不直接处理全量原始数据。
2. AI 只负责识别、判断、消歧和规则草稿补全。
3. 全量数据转换必须由规则引擎执行。
4. 图片输入必须先经过 OCR 和表格结构恢复，再转成统一输入快照。
5. 用户确认前结果必须进入 staging，不能只放内存，也不能直接写正式结果。
6. 当前 PoC 的状态机、DSL、接口和存储结构必须为未来 Java 迁移保留稳定边界。

## 4. 总体架构

```text
[前端]
    |
    v
[FastAPI PoC]
    |
    +-- 上传接口
    +-- 任务接口
    +-- 确认接口
    +-- 预览接口
    +-- 导出接口
    |
    +-- Excel 解析层
    +-- OCR 层
    +-- 知识检索层
    +-- 模板识别 Agent
    +-- 规则草稿 Agent
    +-- 规则引擎
    +-- Staging 预览层
    |
    +-- 数据库
    +-- 文件存储
    +-- 大模型客户端
```

## 5. 统一输入模型

Excel 和图片最终都必须转成统一 `InputSnapshot`：

- `taskId`
- `inputType`
- `sheets`
- `headers`
- `normalizedHeaders`
- `sampleRows`
- `columnStats`
- `headerConfidence`

后续检索和 AI 只依赖这个统一结构，不直接依赖原始文件形态。

补充说明：

- 对当前唯一的“税局网站截图”图片场景，允许直接使用多模态模型提取结构化字段，再落成统一 `InputSnapshot`
- 这是当前 PoC 的显式特例，不是隐藏兜底逻辑

## 6. 知识库设计

本期只做两类知识：

1. 标准模板知识库
2. 场景/国家映射规则知识库

### 6.1 当前 PoC 的模板知识载体

当前 PoC 不再把“每个标准模板 Excel 文件”作为长期运行时事实源。

当前事实源改为：

- `knowledge_base/template_catalog.md`

其中每一行代表一个模板条目，至少包含：

- `templateCode`
- `scene`
- `country`
- 模板全部列名

知识库上传时，用户仍然上传一个 Excel，但这个 Excel 只作为录入材料：

- 第 1 行：目标字段列
- 第 2 行：模板列

系统在上传时会：

1. 解析第 2 行模板列
2. 追加或更新 `template_catalog.md`
3. 基于第 1 行和第 2 行自动生成基础映射规则
4. 将规则写入对应 `scene/country/rule.json`

### 6.2 当前 PoC 的规则知识载体

规则知识继续落在：

- `knowledge_base/<scene>/<country>/rule.json`

当前 PoC 先不做完整规则管理后台，也不强求数据库化知识库。

## 7. 模板检索原则

当前 PoC 的模板识别不再走本地知识库检索打分。

主流程改为：

1. 解析用户输入 Excel 的列名和样本行
2. 读取 `template_catalog.md`
3. 将“输入快照 + 模板目录 markdown”直接提供给大模型
4. 由大模型在 catalog 中判断最相关的 `templateCode / scene / country`

明确要求：

- 模型只能从 catalog 中选择，不允许编造 catalog 之外的模板或场景国家
- 如果多个模板仍然都合理，必须返回 `needUserConfirm=true`
- 文件名和 sheet 名依然不是主识别信号，主信号仍然是列名集合

## 8. AI 职责边界

### 模板识别 Agent

输入：

- 输入快照
- `template_catalog.md`
- 可选的 catalog 摘要

输出：

- `templateCode`
- `sceneCode`
- `countryCode`
- `confidence`
- `alternatives`
- `needUserConfirm`

### 规则草稿 Agent

输出：

- `draftDsl`
- `ambiguousMappings`
- `missingFields`
- `defaultSuggestions`
- `blockingIssues`

AI 不允许发明审批范围外的隐藏成功路径。

### 税局截图图片特例

当前图片场景只有一种映射逻辑，因此允许：

1. 使用 qwen-vl 直接识别税局网站截图
2. 同时输出：
   - 原始可见字段结构
   - 固定目标 schema 的 mapped record
3. 基于固定映射逻辑直接产出预览结果

这个特例只适用于当前 PoC 的税局截图，不推广为通用图片规则。

## 9. DSL 设计

当前只允许以下转换类型：

- `direct`
- `constant`
- `trim`
- `trim_upper`
- `trim_lower`
- `to_number`
- `to_date`
- `enum_map`
- `concat`
- `fallback`

禁止在 DSL 之外偷偷补转换逻辑。

## 10. 单轮确认

所有未决问题都必须在一个确认包中统一呈现，主要包括：

- 模板确认
- 规则确认
- 模糊列名映射确认
- 缺失字段默认值确认
- 需要用户手工修复的源数据问题

## 11. 任务状态机

```text
CREATED
-> PARSING_INPUT
-> INPUT_PARSED
-> TEMPLATE_RETRIEVED
-> RULE_RETRIEVED
-> RULE_DRAFTED
-> WAITING_CONFIRMATION
-> READY_TO_RUN
-> TRANSFORMING
-> PREVIEW_READY
-> FINAL_CONFIRMED
-> EXPORTED
-> FAILED
-> NEED_SOURCE_FIX
```

## 12. 执行与预览

执行原则：

- 禁止用 LLM 逐行处理全量数据
- 禁止长期缓存全量结果到内存
- 禁止确认前写正式结果

预览原则：

- 基于 staging 分页查询
- 支持错误/告警筛选
- 支持行级源数据与目标数据对照

## 13. 未来迁移边界

PoC 阶段必须稳定的部分：

- 输入快照模型
- 知识库结构
- 检索结果结构
- 映射 DSL
- 确认包结构
- 状态机
- staging 结构

## 14. 明确禁止事项

- 用硬编码“智能判断”冒充 AI
- 绕过 `template_catalog.md` 直接强猜模板或规则
- 绕过分层直接改核心逻辑
- 在 DSL 外偷偷加隐藏转换逻辑
- 未经批准擅自修改模型、流程或接口契约
