# 模块结构设计

## 1. 文档目的

本文档定义 Python PoC 的推荐目录和模块职责，用于保证实现严格遵循设计分层。

## 2. 推荐目录结构

```text
data_process/
  app/
    api/
    core/
    domain/
    services/
    repositories/
    models/
    schemas/
    parsers/
    ocr/
    retrieval/
    agents/
    rule_engine/
    staging/
    exporters/
    tasks/
    utils/
    main.py
  scripts/
  reports/
  tests/
    unit/
    integration/
    fixtures/
```

## 3. 模块职责

### `app/api`

- 路由层
- 只负责请求解析、响应组装、调用服务层

### `app/core`

- 基础配置
- 日志
- 依赖注入

### `app/domain`

- 领域枚举与常量

### `app/models`

- ORM 模型
- 对应数据库设计中的表结构

### `app/schemas`

- 接口 DTO
- 输入快照结构
- 确认包结构
- DSL 结构

### `app/repositories`

- 数据访问层
- 只负责读写，不负责业务决策

### `app/parsers`

- Excel 解析层
- 负责表头识别、列名提取、样本抽样、列统计

### `app/ocr`

- OCR 调用和表格结构恢复
- 负责把图片结果转成统一快照

### `app/retrieval`

- 模板/规则检索层
- 负责列名别名匹配、打分、候选结果组装

### `app/agents`

- AI 决策层
- 负责模板识别和规则草稿生成

### `app/rule_engine`

- DSL 校验与转换执行层

### `app/staging`

- 分页预览和汇总层

### `app/exporters`

- 导出层

### `app/services`

- 应用编排层
- 负责上传、解析、检索、确认、执行、导出全流程协调

### `app/tasks`

- 后台任务协调层

### `app/utils`

- 通用技术辅助函数
- 禁止隐藏业务规则

### `scripts`

- PoC 辅助脚本
- 允许承载：
  - 测试数据批量执行
  - 模型对比评测
  - 报告生成
- 禁止在脚本中伪造接口结果
- 脚本必须通过正式 API 或正式服务边界调用系统能力

### `reports`

- PoC 结果产物目录
- 允许存放：
  - Excel 测试结果汇总
  - 多模型对比报告
- 这些文件是演示与评估产物，不是运行时知识库

## 4. 明确禁止的结构捷径

- 在路由中写检索逻辑
- 在 repository 中写转换逻辑
- 让 OCR 适配器直接写 staging 结果
- 在 utils 中隐藏业务规则
- 用临时脚本替代正式模块边界
- 让评测脚本绕过正式接口直接伪造模型判断结果
